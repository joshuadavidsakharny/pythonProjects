# python program / area of triangle

# input variables
base = float(input('Triangle Base: '))
height = float(input('Triangle Height: '))

# calculate / area
area = (base * height) / 2

# output
print("\nArea of Triangle: " + str(area))
